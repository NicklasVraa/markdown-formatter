'use babel';

import MarkdownFormatterView from '../lib/markdown-formatter-view';

describe('MarkdownFormatterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
